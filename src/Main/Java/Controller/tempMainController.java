package Controller;

import javax.swing.JFrame;

public class tempMainController {
    public static void main(String[] args) {
        // Customize JPanel settings as needed...
        // WidgetButtonTower wb = new WidgetButtonTower(null, 0, "Knife",
        // TowerType.knife, null);
        // CreateTowerController controller = new CreateTowerController();

        // // TowerView towervies = new TowerView(controller);
        // JFrame frame = new JFrame();

        // frame.setSize(600, 600);
        // frame.add(controller);
        // frame.setVisible(true);
    }
}